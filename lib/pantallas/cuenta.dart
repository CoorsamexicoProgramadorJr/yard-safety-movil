import 'package:flutter/material.dart';

class cuenta extends StatelessWidget {
  const cuenta({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Pantalla de Login", style: TextStyle(fontSize: 30)),
    );
  }
}
